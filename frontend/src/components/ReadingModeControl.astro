---
import type { Locale } from '../i18n/locales';

interface Props {
  lang: Locale;
}

const { lang } = Astro.props;
const inspectAria = lang === 'ja' ? '詳細表示に切り替える' : 'Switch to inspect view';
const skimAria = lang === 'ja' ? '要約表示に切り替える' : 'Switch to skim view';
---

<button
  class="view-mode-control"
  type="button"
  data-view-mode-toggle
  data-view-mode-skim-aria={skimAria}
  data-view-mode-inspect-aria={inspectAria}
  aria-pressed="false"
  aria-label={inspectAria}
>
  <span>view</span>
  <i aria-hidden="true">/</i>
  <strong data-view-mode-label>skim</strong>
</button>

<script>
  import '../scripts/view-mode.ts';
</script>
